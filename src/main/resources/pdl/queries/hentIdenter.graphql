query($ident: ID!, $gruppe: [IdentGruppe!], $historikk: Boolean = true) {

    hentIdenter(ident: $ident, grupper:$gruppe, historikk:$historikk) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }
}
